module black_mesa_addr::blackmesa {
    use std::debug::print;
    use std::string::{String, utf8};

    const ID: u64 = 100;

    fun set_value(){
        let valueid: u64 = 200;
        let string_val: 
        debug::print(&value_id)
    }
}